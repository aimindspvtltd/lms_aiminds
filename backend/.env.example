# ═══ Database ═══
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lms_db
DB_USERNAME=lms_user
DB_PASSWORD=lms_password

# ═══ JWT ═══
# Minimum 32 characters. Generate with: openssl rand -base64 32
JWT_SECRET=change-this-to-a-random-256-bit-secret-key-minimum-32-chars
JWT_EXPIRY_MS=14400000

# ═══ Admin Bootstrap ═══
# Used by AdminSeeder on first startup to create the initial admin account.
# Change before deploying to production.
ADMIN_EMAIL=admin@lms.internal
ADMIN_PASSWORD=Admin@1234

# ═══ CORS ═══
CORS_ORIGINS=http://localhost:5173
